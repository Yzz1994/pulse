# 节点配置由 install.sh 与 enroll 流程自动写入，包括：
#   PULSE_NODE_ID                 节点 ID（install.sh 写入）
#   PULSE_NODE_SERVER_ADDR        控制面 gRPC host:port（enroll 写入）
#   PULSE_NODE_GRPC_URL           控制面对外 gRPC URL（enroll 写入）
#   PULSE_NODE_CLIENT_CERT_FILE   节点客户端证书（enroll 写入）
#   PULSE_NODE_CLIENT_KEY_FILE    节点客户端私钥（enroll 写入）
#   PULSE_NODE_SERVER_CA_FILE     控制面 CA（enroll 写入）
#
# 一般无需手动编辑本文件。如需自定义可参考 internal/config/config.go。
